# Electricity Checker Bot

## Опис

Electricity Checker Bot - це Telegram бот, який допомагає користувачам стежити за графіками відключення електроенергії в Україні. Бот автоматично парсить інформацію з офіційного сайту https://hoe.com.ua та надає актуальні дані про відключення світла за підчергами.

## Основні функції

### 🌈 RGB-підписка (Преміум функція)
- **Анімований годинник**: Для підписаних користувачів годинник має RGB-анімацію з переливанням кольорів
- **Кольорові сегменти**: Сегменти відключення мають різні кольори та анімацію
- **Покупка за зірки**: Підписка коштує 50 ⭐ Telegram Stars на 30 днів

## Налаштування платежів

### Telegram Stars (Детальна інструкція)

#### 1. Налаштування в BotFather

1. **Перейдіть до [@BotFather](https://t.me/botfather)**
2. **Виберіть вашого бота**:
   ```
   /mybots
   → Виберіть @hoe_electricity_admin_bot
   ```
3. **Перейдіть до налаштувань платежів**:
   ```
   Bot Settings → Payments
   ```
4. **Увімкніть платежі**:
   - Виберіть "Enable payments"
   - Telegram Stars активуються автоматично

#### 2. Налаштування в коді (вже готове)

```python
# В test.py вже налаштовано правильно:
PAYMENT_PROVIDER_TOKEN = ""  # ПУСТО для Stars
CURRENCY = "XTR"  # Telegram Stars
RGB_SUBSCRIPTION_PRICE = 50  # Ціна в зірках
```

#### 3. Важливі моменти

- **PAYMENT_PROVIDER_TOKEN** має бути пустим рядком `""`
- **CURRENCY** має бути `"XTR"`
- **Ціна** вказується в кількості зірочок (не в копійках/центах)

#### 4. Threaded Mode

"Threaded mode" може стосуватися:
- **В BotFather**: Це може бути опція для обробки повідомлень в окремих потоках
- **В коді**: Це може бути налаштування для багатопоточності в aiogram

Для платежів це не обов'язково, але якщо ви бачите цю опцію в BotFather, залиште її увімкненою для кращої продуктивності.

#### 5. Тестування

1. **Запустіть бота**: `python test.py`
2. **Перейдіть до тестового меню**
3. **Натисніть "🌈 Купити RGB-підписку"**
4. **Оплатіть 50 ⭐** (в тестовому режимі не списується)

#### 6. Вимоги для користувачів

- Користувач має бути **Telegram Premium** підписником
- Має мати достатньо **зірочок** на балансі
- Платежі працюють тільки через офіційний Telegram клієнт

#### 7. Можливі проблеми та рішення

**❌ "Payments are not enabled for this bot"**
- Рішення: Перевірте що увімкнули платежі в BotFather

**❌ "Currency not supported"**
- Рішення: Переконайтеся що `CURRENCY = "XTR"`

**❌ "Provider token is invalid"**
- Рішення: Для Stars токен має бути пустим `""`

**❌ Користувач не може оплатити**
- Рішення: Користувач має бути Telegram Premium
- Рішення: Користувач має мати достатньо зірочок

**❌ Threaded mode не працює**
- Це не обов'язково для платежів, ігноруйте якщо не впливає на функціонал

### Інші платіжні системи

#### Portmone
```python
PAYMENT_PROVIDER_TOKEN = "Ваш_Portmone_токен"
CURRENCY = "UAH"
RGB_SUBSCRIPTION_PRICE = 5000  # 50 UAH в копійках
```

#### Stripe
```python
PAYMENT_PROVIDER_TOKEN = "Ваш_Stripe_токен"
CURRENCY = "USD"
RGB_SUBSCRIPTION_PRICE = 200  # 2 USD в центах
```

#### ЮKassa (YooMoney)
```python
PAYMENT_PROVIDER_TOKEN = "Ваш_YooMoney_токен"
CURRENCY = "RUB"
RGB_SUBSCRIPTION_PRICE = 15000  # 150 RUB в копійках
```

### Отримання токенів платіжних систем

1. **Portmone**: Зареєструйтесь на [portmone.com.ua](https://portmone.com.ua)
2. **Stripe**: Створіть акаунт на [stripe.com](https://stripe.com)
3. **YooMoney**: Зареєструйтесь на [yoomoney.ru](https://yoomoney.ru)

### Активація платежів в BotFather

Для кожної платіжної системи:

1. Перейдіть до [@BotFather](https://t.me/botfather)
2. `/mybots` → виберіть бота
3. "Bot Settings" → "Payments"
4. Виберіть платіжну систему
5. Введіть отриманий токен

### Тестові токени

Для тестування можна використовувати:

```python
# Stripe тестовий
PAYMENT_PROVIDER_TOKEN = "284685063:TEST:Z2x0X00Xn4t7R6hY5n3"

# Portmone тестовий
PAYMENT_PROVIDER_TOKEN = "381764678:TEST:1234567890"
```

### Структура платежів

- **Pre-checkout**: Перевірка даних перед оплатою
- **Successful payment**: Активація підписки після оплати
- **Payload**: `rgb_subscription_{user_id}` для ідентифікації платежу

### Безпека

- Перевірка user_id в payload
- Логування всіх платежів
- Fallback на симуляцію при помилках

### Приклад використання

#### Telegram Stars
```python
prices = [LabeledPrice(label="RGB-підписка на 30 днів", amount=50)]
await bot.send_invoice(
    chat_id=user_id,
    title="🌈 RGB-підписка для годинника",
    description="Отримайте анімований годинник з кольоровими сегментами!",
    payload=f"rgb_subscription_{user_id}",
    currency="XTR",
    prices=prices
)
```

#### Portmone (UAH)
```python
prices = [LabeledPrice(label="RGB-підписка на 30 днів", amount=5000)]  # 50 UAH
await bot.send_invoice(
    chat_id=user_id,
    title="🌈 RGB-підписка для годинника",
    description="Отримайте анімований годинник з кольоровими сегментами!",
    payload=f"rgb_subscription_{user_id}",
    provider_token="Ваш_Portmone_токен",
    currency="UAH",
    prices=prices
)
```

#### Stripe (USD)
```python
prices = [LabeledPrice(label="RGB-підписка на 30 днів", amount=200)]  # 2 USD
await bot.send_invoice(
    chat_id=user_id,
    title="🌈 RGB-підписка для годинника",
    description="Отримайте анімований годинник з кольоровими сегментами!",
    payload=f"rgb_subscription_{user_id}",
    provider_token="Ваш_Stripe_токен",
    currency="USD",
    prices=prices
)
```

#### Обробка успішної оплати
```python
@dp.message(F.content_type == "successful_payment")
async def process_successful_payment(message: types.Message):
    payload = message.successful_payment.invoice_payload
    if payload.startswith("rgb_subscription_"):
        user_id = int(payload.split("_")[2])
        add_subscription(user_id, 'rgb', 30)
        await message.answer("🎉 RGB-підписка активована!")
```
        await message.answer("🎉 RGB-підписка активована!")

## Вибір платіжної системи

### Для України 🇺🇦
- **Рекомендовано**: Portmone або Telegram Stars
- **Переваги**: Локальна валюта (UAH), підтримка гривні
- **Налаштування**: `CURRENCY = "UAH"`, ціна в копійках

### Для міжнародних платежів 🌍
- **Рекомендовано**: Stripe або Telegram Stars
- **Переваги**: Підтримка USD/EUR, міжнародні карти
- **Налаштування**: `CURRENCY = "USD"`, ціна в центах

### Telegram Stars ⭐
- **Переваги**: Найпростіше налаштування, вбудовано в Telegram
- **Обмеження**: Тільки для користувачів Telegram Premium
- **Налаштування**: `PAYMENT_PROVIDER_TOKEN = ""`, `CURRENCY = "XTR"`

## Тестування RGB-підписки

### 1. Налаштування тестового токена

```python
# Для тестування додайте в test.py:
PAYMENT_PROVIDER_TOKEN = "284685063:TEST:Z2x0X00Xn4t7R6hY5n3"  # Stripe тестовий
# або
PAYMENT_PROVIDER_TOKEN = "381764678:TEST:1234567890"  # Portmone тестовий
```

### 2. Запуск бота
```bash
python test.py
```

### 3. Тестування покупки
- Перейдіть до "🧪 Тестове меню"
- Натисніть "🌈 Купити RGB-підписку"
- Оплатіть вказану суму (в тестовому режимі не списується)

### 4. Перевірка результату
- Запросіть графік відключень
- Повинен надіслатися GIF з анімацією кольорів

### 5. Тестування видалення підписки
- В тестовому меню є кнопка "❌ Видалити RGB-підписку"
- Можна видалити підписку для повторного тестування покупки

## Важливі нотатки

- **Тестовий режим**: Платежі не списуються з рахунку
- **Продакшен**: Використовуйте справжні токени платіжних систем
- **BotFather**: Обов'язково активуйте платежі для бота
- **Валюта**: Переконайтеся що ціна вказана в правильній валюті (копійки/центи)

## Монетизація через Telegram Stars ⭐

### Куди йдуть гроші від оплати

#### Продакшен режим
Коли бот виведений з тестового режиму:

1. **Гроші надходять на ваш рахунок** в Telegram
2. **Автоматичне зарахування** після успішної оплати
3. **Конвертація**: 1 ⭐ = ~0.01 USD (залежно від курсу)

### Як отримати гроші

#### Варіант 1: Telegram Balance
```
Налаштування → Telegram Premium → Balance
```
- Зірки конвертуються в Telegram Balance
- Можна витратити на Premium або інші покупки в Telegram

#### Варіант 2: Вивід коштів
1. **Верифікуйте аккаунт** розробника в Telegram
2. **Зв'яжіться з підтримкою** @notoscam
3. **Налаштуйте виплати** на банківську карту або PayPal

#### Варіант 3: Партнерські програми
- **Telegram Ads Revenue** - заробляйте на рекламі
- **Bot Monetization Program** - спеціальна програма для ботів

### Курс конвертації

- **1 ⭐ = ~$0.01** (орієнтовно)
- **50 ⭐ = ~$0.50** за підписку
- Курс може змінюватися залежно від регіону

### Важливі нотатки про монетизацію

- **Комісія Telegram**: ~30% від суми платежу
- **Мінімальна сума для виведення**: може бути обмеження
- **Податки**: залежно від вашої країни проживання
- **Документи**: для великих сум потрібна верифікація

### Рекомендації

1. **Почніть з тестування** - переконайтеся що все працює
2. **Виведіть бота з тесту** коли будете готові до монетизації
3. **Вивчайте умови** виплат у документації Telegram
4. **Консультуйтеся** з податковою якщо плануєте заробляти суттєві суми
```
- **Графік на завтра**: Показує графік відключень на завтра (якщо доступний)
- **Загальний графік**: Показує загальний графік з сайту

### 🏠 Керування адресами
- **Декілька адрес**: Кожен користувач може додати кілька адрес з різними назвами та чергами
- **Основна адреса**: Одна адреса позначається як основна для швидкого доступу
- **Меню керування**: Додати, видалити, перейменувати адреси, змінити черги, встановити основну

### ⚡ Автоматичні сповіщення
- **Попередження за 60 хвилин**: Бот надсилає сповіщення за 60 хвилин до початку відключення або відновлення електроенергії
- **Попередження за 15 хвилин**: Додаткове сповіщення за 15 хвилин до зміни
- **Оновлення графіків**: Автоматично повідомляє про появу нових графіків на сайті
- **Детальні зміни**: При змінах у графіку показує що саме змінилося (старий vs новий графік)
- **Об'єднані сповіщення**: Якщо кілька адрес мають однакову чергу, сповіщення об'єднуються в одне повідомлення
- **Перехід між днями**: Правильно обробляє переходи від сьогоднішнього до завтрашнього графіка без дублюючих сповіщень

### 👥 Керування чергою
- Вибір своєї підчерги (від 1.1 до 6.2) для кожної адреси
- Зміна черги в будь-який момент для будь-якої адреси

### 📊 Адмін-функції
- **Розсилка повідомлень**: Надсилання повідомлень всім користувачам (тільки для адміністратора)
- **Статистика бота**: Детальна статистика по користувачам, адресам, сповіщенням (`/stats`)
- **Інспектор бази даних**: Локальний скрипт для детального аналізу бази даних

### 🛠️ Інструменти розробки
- **db_inspector.py**: Скрипт для локального аналізу бази даних бота
- **Детальна статистика**: Інформація про кожного користувача, його адреси та налаштування
- **Аналіз сповіщень**: Історія відправлених сповіщень з групуванням

## Налаштування та безпека

### Змінні середовища
Для безпеки токен бота та ID адміністратора зберігаються в файлі `.env`:

```bash
BOT_TOKEN=ваш_токен_бота
ADMIN_USER_ID=ваш_telegram_user_id
```

### Налаштування сповіщень
Кожен користувач може налаштувати сповіщення окремо для кожної адреси:
- **Загальні налаштування**: Впливають на всі адреси
- **Налаштування для адреси**: Окремо для кожної адреси
- **Типи сповіщень**:
  - Сповіщення про відключення/відновлення світла
  - Нові графіки
  - Зміни в графіках

### Форматування повідомлень
Бот використовує HTML форматування для повідомлень:
- `<b>жирний текст</b>` замість `**жирний текст**`
- `<code>код</code>` замість `` `код` ``
- Підтримує HTML теги для розсилок

## Як працює бот

### Парсинг даних
Бот кожні 2 хвилини перевіряє сайт https://hoe.com.ua на наявність нових графіків. Він шукає зображення з текстом "ГПВ" в alt-атрибуті та витягує відповідні списки відключень з HTML.

### Логіка сповіщень
1. **Новий графік**: При появі нового графіка бот надсилає його всім користувачам з повідомленням про те, що детальні списки будуть додані пізніше
2. **Оновлення**: Коли додаються детальні списки відключень, бот повідомляє про це
3. **30-хвилинні попередження**: Бот аналізує графіки на сьогодні та завтра разом, щоб уникнути дублюючих сповіщень на межі днів

### Обробка часу
- Правильно обробляє 24:00 як початок наступного дня
- Комбінує інтервали відключень з сьогоднішнього та завтрашнього дня для точних сповіщень

## Команди та кнопки

### Основні команди
- `/start` - Початок роботи з ботом, вибір черги
- `/broadcast` - Розсилка повідомлень всім користувачам (тільки для адміна)
- `/stats` - Показати статистику бота (тільки для адміна)

### Клавіатура
- **� Графік на сьогодні** - Показати графік на сьогодні з візуальним годинником для основної адреси
- **🗓️ Графік на завтра** - Показати графік на завтра
- **📊 Загальний графік** - Показати загальний графік з сайту
- **🏠 Керування адресами** - Меню для додавання, редагування та видалення адрес
- **☕ Підтримати бота** - Інформація про підтримку проекту
- **👨‍💻 Зв'язок з розробником** - Контакти розробника

### Інструменти розробки
- **db_inspector.py** - Аналіз бази даних бота локально:
  ```bash
  python db_inspector.py          # аналіз users.db
  python db_inspector.py file.db   # аналіз конкретного файлу
  python db_inspector.py --help    # допомога
  ```

## Встановлення та налаштування

### Вимоги
- Python 3.7+
- Бібліотеки: aiogram, aiohttp, beautifulsoup4, apscheduler, pillow, python-dotenv, sqlite3

### Налаштування
1. Встановіть залежності:
   ```
   pip install aiogram aiohttp beautifulsoup4 apscheduler pillow python-dotenv
   ```

2. Створіть файл `.env` з налаштуваннями:
   ```bash
   BOT_TOKEN=ваш_токен_бота
   ADMIN_USER_ID=ваш_telegram_user_id
   ```

3. Запустіть бота:
   ```bash
   python bot.py
   ```

4. Для аналізу бази даних використовуйте:
   ```bash
   python db_inspector.py
   ```

### База даних
Бот автоматично створює базу даних `users.db` з таблицями:
- `users` - інформація про користувачів (для сумісності)
- `addresses` - адреси користувачів з назвами та чергами
- `user_notifications` - налаштування сповіщень для кожної адреси
- `settings` - налаштування бота
- `sent_alerts` - історія відправлених сповіщень (для уникнення дублів)

### Інструменти аналізу
Для аналізу бази даних використовуйте скрипт `db_inspector.py`:
```bash
python db_inspector.py
```
Скрипт показує:
- Загальну статистику (користувачі, адреси, сповіщення)
- Розподіл по чергах
- Детальну інформацію про кожного користувача з налаштуваннями
- Недавні сповіщення

## Безпека та конфіденційність

- Бот не зберігає особисті дані користувачів, окрім user ID та вибраної черги
- Всі сповіщення відправляються через Telegram API
- Парсинг сайту відбувається анонімно без авторизації

## Недавні оновлення

### v2.0+ (2026)
- **Покращені сповіщення про зміни**: Тепер бот показує конкретні зміни в графіку (старий vs новий графік) замість загального повідомлення
- **Команда /stats**: Адмін-команда для перегляду статистики бота
- **db_inspector.py**: Локальний інструмент для детального аналізу бази даних
- **Покращена обробка фото**: Розрізняє зміни в зображенні від змін у тексті графіка
- **15-хвилинні попередження**: Додаткові сповіщення за 15 хвилин до зміни
- **Краща підтримка кількох адрес**: Покращена логіка для користувачів з кількома адресами

## Підтримка

Якщо у вас виникли питання або проблеми:
- Напишіть розробнику: @denayed
- Підтримайте проект донатом: https://send.monobank.ua/jar/WAXs1bH5s

## Структура проекту

```
electricity-checker/
├── 📁 core/                    # Ядро додатку
│   ├── __init__.py
│   ├── bot.py                 # Ініціалізація бота та планувальника
│   ├── globals.py             # Глобальні змінні (бот)
│   └── states.py              # FSM стани для діалогів
├── 📁 database/               # Робота з базою даних
│   ├── __init__.py
│   ├── connection.py          # Підключення до БД
│   ├── users.py               # Користувачі
│   ├── addresses.py           # Адреси користувачів
│   ├── notifications.py       # Налаштування сповіщень
│   └── schedules.py           # Ручні графіки
├── 📁 handlers/               # Обробники Telegram
│   ├── __init__.py
│   ├── commands.py            # Команди (/start, /stats, /broadcast)
│   ├── callbacks.py           # Callback запити (кнопки)
│   └── messages.py            # Текстові повідомлення
├── 📁 formatting/             # Форматування повідомлень
│   ├── __init__.py
│   ├── keyboard_builder.py    # Клавіатури
│   └── schedule_formatter.py  # Форматування графіків
├── 📁 ocr/                    # Розпізнавання тексту
│   ├── __init__.py
│   ├── parser.py              # Парсинг зображень
│   └── image_processing.py    # Обробка зображень
├── 📁 utils/                  # Допоміжні утиліти
│   ├── __init__.py
│   ├── cache.py               # Кешування даних
│   ├── helpers.py             # Допоміжні функції
│   ├── monitoring.py          # Моніторинг сайтів
│   └── schedule_sender.py     # Надсилання графіків
├── 📁 config/                 # Конфігурація
│   ├── __init__.py
│   ├── settings.py            # Основні налаштування
│   └── constants.py           # Константи
├── 📁 tests/                  # Тестові файли
│   ├── README.md
│   ├── unit/                  # Модульні тести
│   └── integration/           # Інтеграційні тести
├── 📁 dev-tools/              # Інструменти розробки
│   ├── README.md
│   ├── analyze_*.py           # Аналіз даних
│   ├── check_*.py             # Перевірка компонентів
│   ├── inspect_*.py           # Інспекція системи
│   └── test_*.py              # Тестові скрипти
├── 📁 test-data/              # Тестові дані
│   ├── README.md
│   ├── test_*.db              # Тестові БД
│   ├── test_*.png             # Тестові зображення
│   └── test_clocks/           # Тестові годинники
├── 📁 backups/                # Резервні копії
│   ├── README.md
│   ├── bot backup.py          # Резервні версії
│   └── server_*.py            # Серверні файли
├── 📄 run.py                  # Точка входу
├── 📄 requirements.txt        # Залежності
├── 📄 .env.example            # Приклад змінних середовища
└── 📄 README.md               # Цей файл
```

### Опис основних папок

- **`core/`** - Ядро додатку, ініціалізація бота
- **`database/`** - Взаємодія з базою даних SQLite
- **`handlers/`** - Обробники Telegram подій (повідомлення, callback'и)
- **`formatting/`** - Форматування повідомлень та створення клавіатур
- **`ocr/`** - Розпізнавання тексту з зображень графіків
- **`utils/`** - Допоміжні функції (кешування, моніторинг, відправка)
- **`config/`** - Конфігураційні файли та константи
- **`tests/`** - Тестові файли, розділені на unit та integration
- **`dev-tools/`** - Інструменти для розробки та діагностики
- **`test-data/`** - Тестові дані та файли
- **`backups/`** - Резервні копії та альтернативні версії

## Ліцензія

Цей проект поширюється під ліцензією MIT. Деталі дивіться у файлі LICENSE.</content>
<parameter name="filePath">README.md